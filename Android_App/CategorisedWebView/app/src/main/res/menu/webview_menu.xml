<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/webview_reload"
        android:title="Reload"
        android:icon="@drawable/ic_reload"
        app:showAsAction="always"/>

    <item
        android:id="@+id/webview_dark_mode"
        android:title="Dark Mode"
        android:icon="@drawable/ic_dark_mode"
        app:showAsAction="always"/>

    <item
        android:id="@+id/webview_light_mode"
        android:title="Light Mode"
        android:visible="false"
        android:icon="@drawable/ic_light_mode"
        app:showAsAction="always"/>

    <item
        android:id="@+id/webview_desktop_mode"
        android:title="Desktop Mode"
        android:icon="@drawable/ic_desktop_mode"
        android:checkable="true"/>

</menu>